{
  "name": "remote-bridge",
  "version": "2.0.0",
  "private": true,
  "description": "RemoteBridge PHP/MySQL remote support application with optional Windows control agent.",
  "workspaces": [
    "agent"
  ],
  "scripts": {
    "install:agent": "npm install --workspace agent",
    "start:agent": "npm start --workspace agent",
    "check:agent": "node --check agent/control-agent.js"
  }
}
