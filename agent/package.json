{
  "name": "remotebridge-control-agent",
  "version": "1.1.0",
  "description": "Local companion process that injects real mouse/keyboard input for RemoteBridge remote control. Run only on the machine you intend to let a trusted remote user control. Windows-only for now (uses user32.dll via PowerShell, no native compile required).",
  "main": "control-agent.js",
  "scripts": {
    "start": "node control-agent.js"
  },
  "dependencies": {
    "ws": "^8.17.0"
  }
}
